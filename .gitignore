# MkDocs
docs-site/

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg

# Virtual environment
.venv/
venv/

# IDE
.vscode/
.idea/
*.swp

# Environment
.env
.env.*
!.env.example
!.env.*.example

# DB backups (never commit)
*.sql
*.sql.gz

# Secrets / certificates
*.pem
*.key
*.crt
*.p12
credentials*
!docs/architecture/credentials.md
!engramia/credentials/
!engramia/credentials/**
!engramia/api/credentials.py
!tests/test_credentials/
!tests/test_credentials/**
!tests/test_api/test_credentials.py
# Re-ignore __pycache__ which the allow-list above accidentally re-includes
**/__pycache__/

# Brain data (local development)
engramia_data/

# Downloaded benchmark datasets (fetch from source — see
# benchmarks/longmemeval_real.py for the huggingface_hub URL)
benchmarks/data/

# Test
.pytest_cache/
.pytest_tmp*/
.coverage
.coverage.*
.pip-audit-cache/
.cov_live.json
htmlcov/
coverage.xml
tests/recall_quality/results/

# Benchmark outputs (auto-generated runs — but published reference data is tracked)
benchmarks/results/*
!benchmarks/results/longmemeval_*.json
!benchmarks/results/lifecycle_*.json
# task_bench_*.json is intentionally NOT tracked here. Per-run AgentTaskBench
# outputs are operator artifacts and live in the private engramia-ops repo;
# only the harness + methodology ship publicly so anyone can reproduce.

# OS
.DS_Store
Thumbs.db

# Internal / non-public docs
/.claude
CLAUDE.md
docs/legal/key-legal-design-decisions.md

# GitNexus tooling — local-only, never commit anything related to it
.gitnexus/
AGENTS.md
